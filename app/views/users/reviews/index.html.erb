<div class="container" style="margin-top: 50px;">
  <div class="col-md-8 offset-md-2">
    <%#.offset-md-1 は .col-md-9 を 1 カラムぶん右に移動%>
      <div class="shadow p-3 mb-5 bg-white rounded">
        <h2 class="p-3 mb-2 bg-dark text-white">レビュー一覧</h2>
        <h1 class="text-right">
          <div>作品名</div>
          <div>
            <%= @movie.filmname %>
          </div>
          <%= link_to "＋新規追加",new_users_movie_review_path(@movie.id), class: "btn btn-dark rounded-pill" %>
        </h1>
        <p id="notice"></p>
        <table class="table table-hover">
          <tr class="table-secondary">
            <th>タイトル</th>
            <th>ユーザーネーム</th>
            <th>年代</th>
            <th>性別</th>
          </tr>
          <% @reviews.each do |review| %>
            <tr class="table-borderless" data-href="<%= users_movie_review_path(@movie, review) %>">
              <td>
                <%= review.title %>
              </td>
              <!--<td>-->
              <!--  <%#= link_to "詳細へ",users_movie_review_path(@movie,review) %>-->
              <!--</td>-->
              <td><%= review.user.name %></td>
              <td><%= review.user.age %></td>
              <td><%= review.user.gender %></td>
            </tr>
        <% end %>
                <!--<%# @users.each do |user| %>-->
                <!--  <td>-->
                <!--    <%#= user.name %>-->
                <!--  </td>-->
                <!--  <td>-->
                <!--    <%#= user.age %>-->
                <!--  </td>-->
                <!--  <td>-->
                <!--    <%#= user.gender %>-->
                <!--  </td>-->
                  <!--<td> <user画像>
                  <!--  <%# if user.profile_image.present? %>-->
                  <!--    <%#= image_tag user.profile_image,size:"100x100" %>-->
                  <!--      <%# end %>-->
                  <!--</td> -->
                <!--  <%# end %>-->
                    <script>
                      $('#star-all-rate<%#= post.id%>').raty({
                        size      : 36,
                        starOff   : '<%#= asset_path('star-off.png') %>',
                        starOn    : '<%#= asset_path('star-on.png') %>',
                        starHalf  : '<%#= asset_path('star-half.png') %>',
                        readOnly: true,
                        score: <%#= post.all_rate %>,
                      });
                    </script>
            </tr>
        </table>
      </div>
      <ul class="pagination justify-content-center">
        <p>
          <%= paginate @reviews %>
        </p>
      </ul>
  </div>
</div>

<script>
  $('tr[data-href]').click(function () {
     window.location = $(this).attr('data-href');
  });
</script>