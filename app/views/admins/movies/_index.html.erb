<!--タグリスト-->
<% tag_list.each do |list|%>
<%=link_to list.tag_name, admins_search_tag_path(tag_id: list.id) %>
<%="(#{list.movies.count})" %>
<% end %>

<%# movies.each do |movie| %>
  <%#= movie.tags.map(&:tag_name).join(', ') %>
<%# end %>